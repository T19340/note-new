---
name: note-new
description: >-
  한 과목·한 주제를 처음 배우는 사람이 끝까지 읽고 문제까지 풀 수 있게 만드는 장문 한국어
  HTML 강의노트를 짓는 제작 방법론입니다. blueprint 시각 셸(좌본문 + 우측 sticky 목차·참조
  패널, MathJax 클릭-복사)에 실측 인터랙티브 그림(데이터에서 그 자리에서 계산하고 조작하면
  반응하는 캔버스·표·퀴즈)을 얹고, 글을 쓰기 전에 검사 게이트부터 짜서 숫자·구조·문체·렌더를
  기계로 확인한 뒤 납품하는 전 과정을 자산(셸 CSS, 그림 엔진, 조립기, 게이트 스크립트)과 함께
  담고 있습니다. 사용자가 "note-new"를 직접 지목하거나, 이 방법론으로 만든 기존 노트를 가리켜
  "같은 양식으로", "이 노트 이어서 다음 강의" 같이 요청할 때 사용하세요. 형식을 지목하지 않은
  일반 요청(강의노트 만들어줘, 정리해줘)에는 임의로 발동하지 말고 어떤 형식을 원하는지 먼저
  물어보세요. 짧은 메모·슬라이드·한 문단 요약에는 쓰지 않습니다.
license: Proprietary
---

# note-new — 읽고 풀 수 있는 장문 노트 만들기

## 이 스킬이 만드는 것

단일 HTML 파일 하나. 외부 연결 없이 다른 컴퓨터에서도 열리고, 다음 네 가지를 동시에 한다.

1. **읽힌다** — 처음 배우는 사람이 멈추지 않고 끝까지 읽는다. 정의·수식·단서를 줄이지 않고,
   그 둘레에 사전지식·직관·현실 예시·말풀이를 덧대는 방식으로.
2. **만져진다** — 그림은 삽화가 아니라 실험이다. 모든 값은 데이터에서 그 자리에서 계산되고,
   슬라이더·버튼·카드를 움직이면 정직하게 반응한다.
3. **참조된다** — 우측 패널이 스크롤을 따라와 "지금 절의 식과 용어"만 보여 준다.
4. **풀린다** — 문제 유형 지도, 유형별 풀이 틀, 해설이 접힌 연습문제, 시험장 요약 카드까지.
   해설은 답이 아니라 **다음 문제를 혼자 푸는 법**을 적는다 — 무엇을 보고 유형을 알아챘는지,
   왜 그 식을 골랐는지가 계산보다 먼저다(`references/content.md` §9).

`blueprint-study-note`와 `interactive-note`가 각각 시각 셸과 그림 철학의 뿌리다. 이 스킬은
그 둘을 실제 강의노트 제작에 쓰면서 굳어진 **작업 순서와 검증 장치**를 자산과 함께 모아 둔 것이다.
더 깊은 원리가 필요하면 두 스킬의 `references/`를 읽되, 조립·검증은 여기 있는 것으로 한다.

## 작업 순서

### 1. 자료를 먼저 다 읽는다
주어진 폴더의 PDF·슬라이드·문제 해설을 **모두** 훑는다. 텍스트 레이어가 없는 쪽(스캔·이미지)은
PyMuPDF로 렌더해 이미지로 읽는다. 해설지만 있고 문제 원문이 없는 경우처럼 **빠진 자료는 빠졌다고
적어 두고**, 그 공백을 어떻게 메웠는지 노트와 보고에 남긴다. 지어내지 않는다.

사실 확인이 필요한 대목(제도·연도·기관·최신 수치)은 웹으로 확인하고 출처를 참고문헌에 남긴다.
교재·해설지의 오식을 발견하면 고쳐 쓰지 말고 노트 안에 "자료 주의" 박스로 밝힌다. 학생은 결국
그 자료를 보게 되고, 그때 혼란을 막는 것이 노트의 몫이다.

### 2. 문제를 '필요한 지식'으로 갈라 절을 정한다
절을 주제로 나누면 "개념은 다 읽었는데 문제 앞에서는 여전히 막히는" 노트가 된다. 나누는 기준은
**"이 지식을 가지면 이 문제가 풀린다"**이고, 같은 지식으로 풀리는 문제들이 한 단위, **한 단위가
한 절**이다. 문제를 전부 모아 각각 "무엇을 알면 풀리는가"를 적고, 같은 것끼리 묶어 단위마다
지식·단서·대표 예제·문제 전량을 `map.json`에 적는다. 그런 다음 **원자료에서 문제 번호를 직접
세어 빠진 문제가 없는지 기계로 확인한다**(`assets/gates/gate_cover_template.py`,
`assets/map.example.json`). 자세한 절차는 `references/content.md` §8.

절 안의 순서도 같은 원리다 — 필요한 지식 → 알아채는 단서 → 대표 예제 → 그 지식으로 풀리는 문제.
문제를 개념 뒤에 몰아 두지 않고 그 지식 바로 아래에 둔다. 다만 잘 통하는 뼈대가 하나 있다 — **왜 이것이 필요한가 → 가장 단순한 형태 →
확장 → 실제 사례로 읽기 → 문제 풀이**. 자세한 설계 원칙은 `references/content.md`.

### 3. 게이트를 먼저 짜고 그다음 쓴다
이 순서가 이 스킬의 중심이다. 본문을 쓴 뒤 검사하면 "나중에 발견해 고치고 그것을 성과로 보고하는"
패턴이 반복되고, 이미 그 숫자를 전제로 쓴 문장들까지 함께 틀어진다. 그래서 **데이터 → 수치 게이트 →
본문** 순으로 간다. 게이트의 종류와 쓰는 법은 `references/gates.md`, 뼈대는
`assets/gates/gate_numbers_template.py`.

### 4. 조각으로 쓰고 조립한다
본문은 `p0.html`, `p1.html` … 조각으로 쓴다(한 응답에 큰 파일을 통째로 쓰면 출력 상한에 걸려
작업이 통째로 날아간다). 숫자는 `data.json` 한 곳에만 두고 본문·그림·게이트가 모두 그것을 본다.
조립은 `assets/assemble.py` + `note.json` 매니페스트. 기계적인 주의사항은 `references/build.md`.

**조각을 하나 쓸 때마다 그 자리에서 검사한다.**

```
python ../gates/gate_deliver.py --draft p1.html
```

문체·번역투 검사와 통독 장부가 그 조각에만 돈다. 방금 쓴 문장이 장부에 뜨고, 한 줄씩 읽어
`[읽음]`이나 `[고침]`으로 채워야 통과한다. 통과하기 전에 다음 조각으로 넘어가지 않는다.

이 단계를 마지막으로 미루면 안 되는 이유가 있다. 그때는 이미 그 문장을 전제로 뒷글을 써 놓은
뒤라, 고치면 뒷글까지 흔들린다. 흔들리는 게 싫어 결국 그냥 두게 된다. **쓴 자리에서 바로 읽는
것이 가장 싸다.** 여기서 읽어 표시한 문장은 납품 게이트의 장부에 그대로 남으므로, 마지막
통독은 그 뒤에 바뀐 것만 보면 된다.

빈 화면에서 시작하지 말고 `assets/example/`을 새 소스 폴더로 복사해 시작한다. 절 두 개·그림 두 개
짜리 최소 노트지만 조각 구성·패널 카드·scroll-spy·그림 코드·수치 게이트가 모두 연결된 상태로
게이트를 통과한 것이라, 내용만 갈아끼우면 된다.

### 5. 기계로 확인한다
게이트를 모두 돌린 뒤 `assets/gates/render_check.py`로 렌더해 조각 스크린샷을 처음부터 끝까지
훑는다. 조작 장치가 있으면 클릭 시나리오(`assets/gates/itest-template.js`)로 버튼·슬라이더를
눌러 결과 문자열까지 확인한다.

### 6. 사냥 패스 — 문장만 보는 두 번째 정독을 따로 돌린다
**집필과 같은 패스에서 하면 잡히지 않는다.** 방금 쓴 쪽은 자기 습관을 좋은 글로 여기기 때문에,
임무를 "어색한 한국어 고치기" 하나로 좁힌 별도 패스가 필요하다. 이 단계를 건너뛰면 노트는
게이트를 전부 통과하면서도 한국어가 엉망인 채로 납품된다.

`korean_scan.py out.html --triage triage.txt`로 후보를 뽑고 **판정 항목 전량에 판정을 적은 뒤**,
**본문을 처음부터 끝까지 읽는다.** 판정 기록을 채우지 않으면 검사가 통과하지 않는다 —
요약줄의 "HARD 0건"만 보고 넘기는 일이 실제로 있었기 때문이다.

**이 패스는 선택이 아니고, 문장 단위로 강제된다.** 본문을 한 글자라도 고치면 `gate_deliver.py`가
바뀐 문장을 뽑아 `hunt_read.txt`에 한 줄씩 적어 두고, 그 줄들을 `[읽음]`이나 `[고침]`으로 채우기
전에는 납품을 막는다. 고친 직후에 "이 정도는 안 읽어도 된다"고 넘어가는 일이 이 스킬에서 가장
자주 난 사고이고, 그렇게 나간 문장이 실제로 빈 서술("…확률에서는 식 하나로 정해집니다")이었다.
장부를 채우는 동안에는 그 문장을 읽을 수밖에 없다 — 그것이 이 장치의 전부다.

그리고 **한 표현을 고칠 때는 조각·패널·그림 코드를 전부 검색해 모든 자리를 고친다.** 본문만
고치고 캡션·패널·그림 코드에 같은 표현을 남긴 사고가 이 스킬에서 세 번 났다. 고친 뒤
`grep`으로 그 표현이 0건인지 확인하고 기록에 적는다. 린터는 알려진 패턴만 잡는다 — 지워도 되는 문장, 글쓴이만 아는 은어, 쓸데없이 쪼갠
문장, 그리고 **문장의 수준**은 읽어야만 드러난다. 기준과 절차는 `references/hunt-pass.md`.

**문장의 수준은 내용과 나란한 평가 기준이다.** 뜻이 통하는 것은 최저 조건이지 합격선이 아니다.
문단마다 "이 문장이 하는 일이 무엇인가"를 묻고, 답이 없으면 줄이지 말고 지운다. 부제·요지만이
아니라 절을 여는 문장·캡션·표 앞뒤·해설·패널 카드까지 같은 기준으로 본다. 네 질문과 고친 예는
`references/content.md` 머리말, 읽는 요령은 `hunt-pass.md` ⑫.

**개념어와 일상어를 섞지 않는다.** 개념이 있는 자리에 일상 비유를 놓으면(예: "서로 독립이
아니다"를 "서로 얽혀 있다"로) 독자는 뜻을 고를 수 없고, 한 노트가 같은 것을 두 이름으로 부르게
된다. 개념어는 처음 쓰는 자리에서 **원어를 밝히고**(독립(independent)), 일상어에도 있는
낱말은(독립·상관·기대·정규·유의·표본) 정의와 함께 일상 뜻과 어디가 다른지를 적는다.
`references/content.md` §11, `hunt-pass.md` ⑬.

### 7. 납품하고 재현 가능하게 남긴다
**`gate_deliver.py`가 통과해야 납품한다.** 구조·문체·읽는 순서·번역투·수식·수치·사냥 패스
기록을 **한 번에, 전부, 조립본 전체를 대상으로** 돌린다. 조각마다 이미 돌렸더라도 다시 돌린다 —
조각에는 없던 글(마스트헤드, 패널, `<title>`, 그림 코드의 화면 문자열)이 조립본에만 있다.

**검사 범위는 노트의 모든 글자다.** 문체 검사는 매번 "한글 N자 중 M자를 보았다"를 찍고, 하나라도
못 본 글자가 있으면 실패한다. 한때 `p·li·h·td·th·figcaption`만 보느라 노트 한글의 16%가 검사
밖이었고, 거기에 다른 노트에서 따라온 `<title>`과 구어체가 섞인 버튼 라벨이 들어 있었다.
**화이트리스트는 빠뜨린 것을 조용히 만든다.** 골라 돌리는 선택지를 두지 않는 이유는
실측이다 — "돌리면 좋은 것"으로 적혀 있던 검사는 압박이 오면 빠졌고, 실제로 그렇게 빠진 린터
하나가 납품본에서 하드 위반을 품고 있었다. 사냥 패스 기록이 지금 빌드와 짝이 맞는지까지
확인하므로, 본문을 고치면 기록이 낡아 다시 통독하게 된다.

완성 HTML과 **소스 조각·게이트·매니페스트를 함께** 저장하고, 저장한 소스로 다시 조립한 파일이
납품본과 같은지 확인한다(`cmp`). 다음 강의를 이어 만들 때 이것이 있으면 데이터와 그림을 그대로
확장할 수 있다.

## 사용자에게 말할 때

이 스킬은 제 나름의 작업 용어를 만든다 — 조각, 소스 폴더, 게이트, 판정 기록, 사냥 패스,
통독 장부. **그 말을 설명 없이 사용자에게 던지지 않는다.** 실제로 "소스를 주시겠습니까"라고
물었다가 "소스가 뭔데? 알려준 적도 없으면서"라는 지적을 받았다. 노트 안의 은어를 금지하는
것과 같은 이유다 — 듣는 쪽 머릿속에 없는 말은 전달이 아니라 해독 요구다.

처음 쓰는 자리에서 한 마디로 풀거나(“소스 조각(노트를 나눠 쓴 HTML 파일들)”), 아예 풀어 쓴
말로 바꾼다. 무엇을 해 달라고 부탁할 때는 특히 그렇다 — 상대가 무엇을 주어야 하는지 모르면
부탁이 성립하지 않는다.

## 참고 파일

| 파일 | 언제 읽나 |
|---|---|
| `references/content.md` | 절 구조와 설명 방식을 정할 때. 머리말의 문장 수준 기준(모든 문단에 묻는 네 질문), 개념어와 일상어를 섞지 않는 법(원어 병기·겹침 낱말), 진화의 사슬, 하나의 데이터 관통, 오해 차단, 실무 박스, 시험 대비 층(유형 지도·풀이 틀·쌍둥이 문제·요약 카드), 한국어 문체 규칙 |
| `references/figures.md` | 그림을 설계할 때. 그림이 갖춰야 할 조건과, 이미 검증된 인터랙션 아홉 가지의 용도·데이터 계약 |
| `references/gates.md` | 작업 시작 직후. 게이트 여섯 종과 각각이 잡는 결함, 수치 게이트를 새 주제에 맞게 쓰는 법 |
| `references/hunt-pass.md` | 집필이 끝난 직후. 사냥 패스의 열세 유형(극적 제시·군더더기 문장·은어·쪼갠 문장·관사 '하나/한'·다의어 고정·유정물 전용 말·영어 주어·명사 겹싸기·번역투·감정 강요·문장의 수준·개념어 자리에 들어온 일상어)과 절차 |
| `references/build.md` | 조립·납품 단계. 매니페스트, id 충돌·대문자 변환·svg 스코핑 같은 함정, 오프라인 요건 |
| `SOURCES.md` | 이 방법론의 각 조각이 어디서 왔는지 추적할 때 |

## 자산

- `assets/shell/` — blueprint 시각 셸: `head_css.html`(디자인 토큰·패널·컴포넌트 CSS),
  `components.css`(퀴즈 카드·FSET/원장 표·분개표·T계정·영어 문제 박스·요약 카드),
  `eq-copy-and-mathjax-config.html`(수식 클릭-복사), `mathjax-tex-svg-inline.html`(2.1MB 인라인
  MathJax — 오프라인 요건), `_sample_head_extra.html`(마스트헤드 예시).
- `assets/js/` — `vz-core.html`(의존성 0 캔버스 라이브러리), `common.js`(숫자 포맷, 카드 퀴즈
  엔진, 사용자 눈금 축, 폭포 그림).
- `assets/gates/` — `gate_struct.py`, `gate_numbers_template.py`, `render_check.py`,
  `audit_prep.py`, `texscan.py`, `korean_scan.py`(문체 검사 — HARD·판정·통계 세 층,
  제목·표 칸까지 본다), `check_content_style.py`(번역체·AI 결함을 하드/소프트로 구분),
  `gate_deliver.py`(납품 게이트 — 모든 검사와 사냥 패스 기록·통독 장부를 묶어서 본다.
  `--draft`로 조각 하나만 검사한다), `gate_order.py`(읽는 순서),
  `fix_control_chars.py`(헤어독이 먹은 백슬래시 복구 — 조각을 셸로 고친 직후 돌린다),
  `itest-template.js`.
- `assets/assemble.py`, `assets/note.example.json` — 조립기와 매니페스트 예시.
- `assets/example/` — 위 자산만으로 조립되는 최소 노트 한 벌(조각·패널·spy·그림·데이터·수치
  게이트·용어 파일). `python ../assemble.py note.json`으로 그 자리에서 다시 조립된다.
- `assets/terms.example.txt` — 구조 게이트에 넘기는 필수 용어 목록의 예시와 고르는 기준.

## 이 방법론이 잘 맞지 않는 곳

한 화면짜리 메모, 슬라이드, 짧은 요약에는 과하다. 원본 자료를 형식만 바꿔 옮기는 변환 작업이라면
본문을 다시 쓰지 말고 `blueprint-study-note`의 변환 규칙을 따른다. 실행 가능한 코드가 주인공인
주제라면 동반 노트북이 필요하므로 `blueprint-study-note`의 `references/workbook.md`를 함께 본다.
